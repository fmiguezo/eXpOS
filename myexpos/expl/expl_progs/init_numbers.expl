int main()
{
decl
    int temp,num; //declaro dos variables de tipo entero para poder operar
enddecl
begin
    num = 1; //inicializo la variable num en 1
//imprimo números enteros hasta llegar a 100 inclusive
    while ( num <= 100 ) do 
         temp = exposcall ( "Write" , -2, num );//hago una llamada a write para imprimir el número correspondiente
         num = num + 1; //incremento la variable num para poder continuar con la impresión de números
    endwhile;
    return 0;
end
}
